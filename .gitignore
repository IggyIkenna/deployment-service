# --- Keep these (do not ignore): uv.lock, package.json, .env.example, tsconfig.json ---
# --- Repo-specific exceptions (add below; sync preserves this section) ---
.readiness
# Foundry/Forge build artifacts
contracts/cache/
contracts/lib/
contracts/out/
contracts/.gas-snapshot

# =============================================================================
# Central .gitignore — workspace SSOT (Python + React/TS + security)
# Copy to repo root as .gitignore. Source: unified-trading-pm/scripts/templates/
# =============================================================================

# --- Security (never commit credentials) ---
*credentials*.json
*-credentials.json
*-service-account.json
workspace-dev-sa-key.json
service-account*.json
*secret*.json
central-element-323112-*.json
.env
.env.local
.env.*.local
.env.production
**/secrets/
**/credentials/
*.p12
*.json.bak

# --- OS / editor ---
.DS_Store
.DS_Store?
Thumbs.db
*.backup
*~
*.swp
*.swo
.idea/
.vscode/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.hypothesis/
.ruff_cache/
.cache/
*.egg-info/
.eggs/
*.egg
*.so
.Python
.tox/
.nox/
.pyre/
.pytype/
.ropeproject
.spyderproject
.spyproject

# --- Python venvs ---
.venv/
.venv*/
venv/
venv*/
.venv-workspace/
ENV/
env/

# --- Node / React / TypeScript ---
node_modules/
.next/
dist/
build/
.turbo/
.nyc_output/

# --- Coverage / test artifacts ---
.coverage
.coverage.*
coverage/
coverage.xml
htmlcov/
nosetests.xml

# --- QG sentinels (local-only caches written by quality-gates.sh; NEVER commit — they churn
#     the worktree + jam slot-cron-ff-pull; HARD RULE codified 2026-06-03) ---
.qg_last_passed_sha
.qg_content_sentinel

# --- Data / logs (repo-local, not for git) ---
data/
logs/
*.log

# --- Common data / document file types (generated or local, not for git) ---
# Uncomment or add !tests/fixtures/*.csv if the repo commits CSV fixtures
*.csv
*.xlsx
*.xls
*.xlsm
*.ppt
*.pptx
*.pptm
*.doc
*.docx
*.docm
*.pdf
*.parquet
*.tsv
*.ods
*.odt
*.odp

# --- Terraform (active in this repo) ---
**/.terraform/
*.tfstate
*.tfstate.*
# Lambda build artifacts generated by archive_file data source
terraform/aws/api_host_auto_reboot.zip
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.auto.tfvars

# --- Optional: Jupyter (uncomment if repo has notebooks) ---
# .ipynb_checkpoints/
# profile_default/

# Claude Code agent worktrees — gitlinks that leak as phantom submodules in CI (never track)
.claude/worktrees/
