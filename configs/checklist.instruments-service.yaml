# Deployment readiness delta — canonical compliance checklist: unified-trading-codex/10-audit/batch/checklist.instruments-service.yaml
service_name: instruments-service
last_updated: '2026-02-10'
owner: ''
execution_order: 1
phase_1_repository_foundation:
  item_06_dockerfile_exists:
    description: Containerization ready for Cloud Build
    status: done
    notes: Multi-stage build, non-root user, health check, installs unified-trading-library
      from GitHub
    verified_date: '2026-01-27'
phase_2_testing_and_quality:
  item_11_cloud_build_config:
    description: cloudbuild.yaml includes quality gates before Docker build
    status: done
    notes: Gets GitHub token, clones unified-trading-library, runs ruff + pytest, builds
      and pushes image
    verified_date: '2026-01-27'
  item_12_quality_gates_pass:
    description: Pushing to main triggers quality gates; failure blocks image build
    status: done
    notes: 'Quality gates pass. Latest: PR #11 merged, Cloud Build bd9fbcb SUCCESS
      (2026-02-08T12:07:51Z, asia-northeast1). Image: asia-northeast1-docker.pkg.dev/test-project/instruments/instruments-service:bd9fbcb'
    verified_date: '2026-02-08'
phase_3_deployment_infrastructure:
  item_13_sharding_config:
    description: configs/sharding.instruments-service.yaml exists
    status: done
    notes: Defines category (fixed), date (date_range) dimensions with daily granularity
    verified_date: '2026-01-27'
  item_14_dependencies_entry:
    description: Entry in configs/dependencies.yaml
    status: done
    notes: Root service with no upstream dependencies. Outputs to instruments-store-{category_lower}
      bucket
    verified_date: '2026-01-27'
  item_15_expected_start_dates:
    description: Entry in configs/expected_start_dates.yaml
    status: done
    notes: 'CEFI: 2017-01-01, TRADFI: 2020-01-01, DEFI: 2020-12-18. Venue-specific
      dates defined'
    verified_date: '2026-01-27'
  item_16_venues_entry:
    description: Service's venues in configs/venues.yaml
    status: done
    notes: 'Venue dimension configured in sharding.instruments-service.yaml as hierarchical
      (type: hierarchical, parent: category, source: venues.yaml)'
    verified_date: '2026-02-04'
  item_17_terraform_resources:
    description: Buckets, IAM, Cloud Run job defined in terraform/
    status: done
    notes: 'terraform plan shows ''No changes''. Cloud Run Job, Daily Workflow, Backfill
      Workflow deployed. GCS buckets exist: instruments-store-{cefi,tradfi,defi}-test-project'
    verified_date: '2026-01-27'
  item_17b_required_buckets_exist:
    description: All required input/output buckets exist before deployment
    status: done
    notes: Root service - no input buckets required. Output buckets verified.
    verified_date: '2026-01-28'
    gcp_buckets:
      outputs:
      - instruments-store-cefi-test-project
      - instruments-store-tradfi-test-project
      - instruments-store-defi-test-project
      inputs: []
    aws_buckets:
      outputs:
      - unified-trading-instruments-cefi-${AWS_ACCOUNT_ID}
      - unified-trading-instruments-tradfi-${AWS_ACCOUNT_ID}
      - unified-trading-instruments-defi-${AWS_ACCOUNT_ID}
      inputs: []
  item_18_deploy_cli_works:
    description: python deploy.py deploy --service instruments-service executes without
      errors
    status: done
    notes: deploy.py info and calculate commands work. Sharding correctly splits by
      category (CEFI, TRADFI, DEFI) and date.
    verified_date: '2026-01-27'
phase_5_production_deployment:
  item_23_image_builds:
    description: Cloud Build produces image in Artifact Registry
    status: done
    notes: 'Images exist in asia-northeast1-docker.pkg.dev/test-project/instruments/instruments-service.
      Builds auto-trigger in asia-northeast1 region (NOT global). Latest successful
      build: 2026-02-08T12:07:51Z (commit bd9fbcb, PR #11 hardening). Image tag: bd9fbcb'
    verified_date: '2026-02-08'
  item_24_deployment_runs_smoothly:
    description: Full deployment completes in reasonable time (<1 hour)
    status: done
    notes: Infrastructure verified. Daily workflow at 8:30 AM UTC. Live workflow every
      15 min (Cloud Scheduler */15 * * * *) - single-cycle Cloud Run to avoid wasting
      compute. Shards ~5 min each per estimate.
    verified_date: '2026-02-22'

phase_9_deployable_enhancements:
  item_38_data_availability:
    description: "Data availability verified — input/output buckets exist; data paths verified"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "All required GCS buckets exist (inputs and outputs)"
      - "Data paths align with data-catalogue.instruments-service.yaml"
      - "At least one successful read/write cycle verified"
    reference: "plans_to_deployable_unified_audit.plan.md Phase 10"

  item_39_gap_filling:
    description: "Empty gaps documented; recovery process defined for known gaps"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "Known data gaps documented (holidays, pre-launch, venue not live)"
      - "Gap-filling process defined for recoverable gaps"
    reference: "plans_to_deployable_unified_audit.plan.md checklist-enhancements"

  item_40_recovery_processes:
    description: "Recovery processes documented for failure scenarios"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "Recovery runbook exists for common failure modes"
      - "Backfill/re-run process documented"
    reference: "plans_to_deployable_unified_audit.plan.md checklist-enhancements"

  item_41_security_audit_trail:
    description: "Security audit trail for security-relevant actions"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "Secret access logged (SECRET_ACCESSED event)"
      - "Auth failures logged (AUTH_FAILURE event)"
      - "Config changes logged (CONFIG_CHANGED event)"
    reference: "event-logging.mdc, trading-system-audit-prompt.md"
