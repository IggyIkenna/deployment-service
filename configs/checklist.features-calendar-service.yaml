# Deployment readiness delta — canonical compliance checklist: unified-trading-codex/10-audit/batch/checklist.features-calendar-service.yaml
service_name: features-calendar-service
last_updated: '2026-02-10'
owner: ''
execution_order: 4
phase_1_repository_foundation:
  item_06_dockerfile_exists:
    description: Containerization ready for Cloud Build
    status: done
    notes: Multi-stage build with unified-trading-library integration, non-root user
    verified_date: '2026-01-27'
phase_2_testing_and_quality:
  item_11_cloud_build_config:
    description: cloudbuild.yaml includes quality gates before Docker build
    status: done
    notes: Includes quality gates (lint + tests) before Docker build, proper Secret
      Manager integration
    verified_date: '2026-01-27'
  item_12_quality_gates_pass:
    description: Pushing to main triggers quality gates; failure blocks image build
    status: done
    notes: ruff check passes (all checks passed)
    verified_date: '2026-02-03'
phase_3_deployment_infrastructure:
  item_13_sharding_config:
    description: configs/sharding.features-calendar-service.yaml exists
    status: done
    notes: Defines category, date dimensions
    verified_date: '2026-01-27'
  item_14_dependencies_entry:
    description: Entry in configs/dependencies.yaml
    status: done
    notes: Root service with no upstream dependencies. Outputs temporal_features,
      scheduled_events, event_actuals
    verified_date: '2026-01-27'
  item_15_expected_start_dates:
    description: Entry in configs/expected_start_dates.yaml
    status: done
    notes: 'CEFI: 2017-01-01, TRADFI: 2020-01-01, DEFI: 2020-12-17'
    verified_date: '2026-01-27'
  item_16_venues_entry:
    description: Service's venues in configs/venues.yaml
    status: n/a
    notes: No hierarchical venue dimension
    verified_date: '2026-01-27'
  item_17_terraform_resources:
    description: Buckets, IAM, Cloud Run job defined in terraform/
    status: done
    notes: Created terraform/services/features-calendar-service/gcp/ with main.tf,
      variables.tf, backend.tf, terraform.tfvars, outputs.tf
    verified_date: '2026-01-27'
  item_17b_required_buckets_exist:
    description: All required input/output buckets exist before deployment
    status: pending
    notes: Root service - no input buckets required. Output buckets need verification.
    verified_date: null
    gcp_buckets:
      outputs:
      - features-calendar-cefi-test-project
      - features-calendar-tradfi-test-project
      - features-calendar-defi-test-project
      inputs: []
    aws_buckets:
      outputs:
      - unified-trading-features-calendar-cefi-${AWS_ACCOUNT_ID}
      - unified-trading-features-calendar-tradfi-${AWS_ACCOUNT_ID}
      - unified-trading-features-calendar-defi-${AWS_ACCOUNT_ID}
      inputs: []
  item_18_deploy_cli_works:
    description: python deploy.py deploy --service features-calendar-service executes
    status: done
    notes: 'Verified: deploy.py info, calculate commands work. Service properly configured
      with sharding.'
    verified_date: '2026-01-27'
phase_5_production_deployment:
  item_23_image_builds:
    description: Cloud Build produces image in Artifact Registry
    status: partial
    notes: 'BLOCKED: Quality gates failing since 2026-01-27. No new images built.
      Fix ruff linting errors to unblock. Builds in asia-northeast1 region (NOT global).'
    verified_date: '2026-01-31'
  item_24_deployment_runs_smoothly:
    description: Full deployment completes in reasonable time (<1 hour)
    status: done
    notes: Deployment completed successfully in 49 seconds for 2 shards (CEFI Jan
      2024)
    verified_date: '2026-01-27'

phase_9_deployable_enhancements:
  item_38_data_availability:
    description: "Data availability verified — input/output buckets exist; data paths verified"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "All required GCS buckets exist (inputs and outputs)"
      - "Data paths align with data-catalogue.features-calendar-service.yaml"
      - "At least one successful read/write cycle verified"
    reference: "plans_to_deployable_unified_audit.plan.md Phase 10"

  item_39_gap_filling:
    description: "Empty gaps documented; recovery process defined for known gaps"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "Known data gaps documented (holidays, pre-launch, venue not live)"
      - "Gap-filling process defined for recoverable gaps"
    reference: "plans_to_deployable_unified_audit.plan.md checklist-enhancements"

  item_40_recovery_processes:
    description: "Recovery processes documented for failure scenarios"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "Recovery runbook exists for common failure modes"
      - "Backfill/re-run process documented"
    reference: "plans_to_deployable_unified_audit.plan.md checklist-enhancements"

  item_41_security_audit_trail:
    description: "Security audit trail for security-relevant actions"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "Secret access logged (SECRET_ACCESSED event)"
      - "Auth failures logged (AUTH_FAILURE event)"
      - "Config changes logged (CONFIG_CHANGED event)"
    reference: "event-logging.mdc, trading-system-audit-prompt.md"
