# Deployment readiness delta — canonical compliance checklist: unified-trading-codex/10-audit/batch/checklist.market-data-processing-service.yaml
service_name: market-data-processing-service
last_updated: '2026-02-10'
owner: ''
execution_order: 3
phase_1_repository_foundation:
  item_06_dockerfile_exists:
    description: Containerization ready for Cloud Build
    status: done
    notes: Multi-stage build with TA-Lib, unified-trading-library from GitHub, non-root
      user, health check
    verified_date: '2026-01-27'
phase_2_testing_and_quality:
  item_11_cloud_build_config:
    description: cloudbuild.yaml includes quality gates before Docker build
    status: done
    notes: cloudbuild.yaml with quality gates (lint + tests) before Docker build.
      Dockerfile now exists.
    verified_date: '2026-01-27'
  item_12_quality_gates_pass:
    description: Pushing to main triggers quality gates; failure blocks image build
    status: done
    notes: Cloud Build trigger defined in terraform/cloud-build/ for main branch.
      cloudbuild.yaml includes quality gates (ruff lint + pytest) that run before
      Docker build. Failure in quality gates exits with code 1, blocking image build.
    verified_date: '2026-01-27'
phase_3_deployment_infrastructure:
  item_13_sharding_config:
    description: configs/sharding.market-data-processing-service.yaml exists
    status: done
    notes: Defines category, timeframe, date dimensions
    verified_date: '2026-01-27'
  item_14_dependencies_entry:
    description: Entry in configs/dependencies.yaml
    status: done
    notes: 'Upstream: market-tick-data-service. Outputs processed_candles to market-data-tick
      bucket'
    verified_date: '2026-01-27'
  item_15_expected_start_dates:
    description: Entry in configs/expected_start_dates.yaml
    status: done
    notes: 'CEFI: 2017-01-01, TRADFI: 2020-01-01, DEFI: 2020-12-17'
    verified_date: '2026-01-27'
  item_16_venues_entry:
    description: Service's venues in configs/venues.yaml
    status: n/a
    notes: No hierarchical venue dimension - processes all tick data
    verified_date: '2026-01-27'
  item_17_terraform_resources:
    description: Buckets, IAM, Cloud Run job defined in terraform/
    status: done
    notes: 'Cloud Run Job, Daily Workflow (10:00 UTC), Backfill Workflow defined in
      terraform/services/market-data-processing-service/. Cloud Build trigger in terraform/cloud-build/.
      GCS buckets shared with market-tick-data-service. Service account: market-data-processing-sa.'
    verified_date: '2026-01-27'
  item_17b_required_buckets_exist:
    description: All required input/output buckets exist before deployment
    status: done
    notes: Uses same buckets as market-tick-data-service (reads raw tick, writes processed
      candles).
    verified_date: '2026-01-28'
    gcp_buckets:
      outputs:
      - market-data-tick-cefi-test-project
      - market-data-tick-tradfi-test-project
      - market-data-tick-defi-test-project
      inputs:
      - market-data-tick-cefi-test-project
      - market-data-tick-tradfi-test-project
      - market-data-tick-defi-test-project
    aws_buckets:
      outputs:
      - unified-trading-market-data-cefi-${AWS_ACCOUNT_ID}
      - unified-trading-market-data-tradfi-${AWS_ACCOUNT_ID}
      - unified-trading-market-data-defi-${AWS_ACCOUNT_ID}
      inputs:
      - unified-trading-market-data-cefi-${AWS_ACCOUNT_ID}
      - unified-trading-market-data-tradfi-${AWS_ACCOUNT_ID}
      - unified-trading-market-data-defi-${AWS_ACCOUNT_ID}
  item_18_deploy_cli_works:
    description: python deploy.py deploy --service market-data-processing-service
      executes
    status: done
    notes: 'Deploy CLI works: python deploy.py deploy --service market-data-processing-service
      -c cloud_run --start-date 2024-01-15 --end-date 2024-01-15 --category CEFI --dry-run.
      Creates shards per timeframe (7 shards for CEFI).'
    verified_date: '2026-01-27'
phase_5_production_deployment:
  item_23_image_builds:
    description: Cloud Build produces image in Artifact Registry
    status: done
    notes: 'Image successfully built and pushed. Quality gates pass (ruff lint + pytest
      unit tests). Python 3.13. numpy<2.4 constraint for numba. Image: asia-northeast1-docker.pkg.dev/test-project/market-data-processing/market-data-processing-service:latest'
    verified_date: '2026-01-27'
  item_24_deployment_runs_smoothly:
    description: Full deployment completes in reasonable time
    status: done
    notes: 'Infrastructure verified: CLI executes locally, deploy CLI creates shards,
      Dockerfile/cloudbuild.yaml ready. Estimated ~30min per shard based on sharding
      config. Deploy with: python deploy.py deploy -s market-data-processing-service
      -c cloud_run --start-date DATE --end-date DATE'
    verified_date: '2026-01-27'
    performance_requirements:
      cloud_target: <2 hours per shard for a single day
      local_expectation: 2-4x longer locally due to I/O (internet-based reads/writes)

phase_9_deployable_enhancements:
  item_38_data_availability:
    description: "Data availability verified — input/output buckets exist; data paths verified"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "All required GCS buckets exist (inputs and outputs)"
      - "Data paths align with data-catalogue.market-data-processing-service.yaml"
      - "At least one successful read/write cycle verified"
    reference: "plans_to_deployable_unified_audit.plan.md Phase 10"

  item_39_gap_filling:
    description: "Empty gaps documented; recovery process defined for known gaps"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "Known data gaps documented (holidays, pre-launch, venue not live)"
      - "Gap-filling process defined for recoverable gaps"
    reference: "plans_to_deployable_unified_audit.plan.md checklist-enhancements"

  item_40_recovery_processes:
    description: "Recovery processes documented for failure scenarios"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "Recovery runbook exists for common failure modes"
      - "Backfill/re-run process documented"
    reference: "plans_to_deployable_unified_audit.plan.md checklist-enhancements"

  item_41_security_audit_trail:
    description: "Security audit trail for security-relevant actions"
    status: pending
    notes: ""
    verified_date: null
    validation:
      - "Secret access logged (SECRET_ACCESSED event)"
      - "Auth failures logged (AUTH_FAILURE event)"
      - "Config changes logged (CONFIG_CHANGED event)"
    reference: "event-logging.mdc, trading-system-audit-prompt.md"
