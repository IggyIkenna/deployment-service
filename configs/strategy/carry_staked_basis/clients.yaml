# Per-client configuration for carry_staked_basis archetype, shard 0.
# Schema: unified_api_contracts.canonical.domain.strategy.ClientsYaml
# Validated by StrategySupervisor at boot via UAC ClientsYaml.model_validate_yaml().
#
# May-23 ships with shard_id=0 for all clients. Multi-shard (Phase E.2) is 2026-05-28.
# venue_creds_kms_path format: projects/{project}/secrets/{secret}/versions/{version}
clients:
  - client_id: "us"
    shard_id: 0
    venue_creds_kms_path: "projects/central-element-323112/secrets/venue-creds-us-carry/versions/latest"
    min_balance_per_venue:
      binance: 1000.0
      hyperliquid: 500.0
      aave-v3: 500.0
      lido: 0.0
    risk_limits:
      max_position_usd: 50000.0
      max_drawdown_pct: 0.05
      max_order_size_usd: 10000.0

  - client_id: "defi-client-1"
    shard_id: 0
    venue_creds_kms_path: "projects/central-element-323112/secrets/venue-creds-defi-client-1-carry/versions/latest"
    min_balance_per_venue:
      binance: 500.0
      hyperliquid: 250.0
      aave-v3: 250.0
      lido: 0.0
    risk_limits:
      max_position_usd: 25000.0
      max_drawdown_pct: 0.05
      max_order_size_usd: 5000.0
