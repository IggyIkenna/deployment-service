name: Quality Gates Test

on:
  push:
    branches: [live-defi-rollout]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from minimal workflow on $(uname -a)"
