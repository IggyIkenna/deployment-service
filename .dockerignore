# Git
.git
.gitignore

# Python
__pycache__
*.py[cod]
*$py.class
*.egg-info
.eggs
dist
build
.venv
venv
.pytest_cache
.ruff_cache
.coverage
htmlcov

# Node (UI has its own node_modules in COPY)
ui/node_modules

# IDE
.idea
.vscode
*.swp
*.swo

# Terraform
terraform/
*.tfstate
*.tfstate.*
*.tfplan
.terraform

# Docs
docs/

# Tests + scripts: NOT excluded — the api-dev stage in Dockerfile copies both
# (tests/ for in-image quality-gates, scripts/ for QG helpers). The api stage
# does not COPY them, so they don't bloat the production image.

# Templates (not needed at runtime)
templates/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Environment
.env
.env.*

# Build artifacts
*.tar
*.tar.gz
